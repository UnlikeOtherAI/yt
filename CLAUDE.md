# Claude Instructions

- Commit and push after each turn.
- Do not leave uncommitted files in the working tree unless they are
  autogenerated and ignored by `.gitignore`.
- If a file is expected to be generated repeatedly, add it to `.gitignore`
  instead of leaving it unstaged.
